# Event Crusher Skill

This directory contains the OpenCode skill definition for Event Crusher.

## Install
1. Symlink:
   `ln -s ~/projects/opencode-event-crusher/skill ~/.config/opencode/skill/events`
2. Restart OpenCode.

## Usage
Use the `events` skill to invoke `oc-events` commands.
