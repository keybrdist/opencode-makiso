# Skill Install

1. Symlink into OpenCode skills:
   `ln -s ~/projects/opencode-event-crusher/skill ~/.config/opencode/skill/events`

2. Restart OpenCode.

3. Verify skill:
   Use the skill to run `oc-events` commands (push, pull, query).
